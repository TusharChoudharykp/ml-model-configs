metadata:
  name: app-deploy-prediction
  labels:
    team: testing team
    owner: app@gmail.com
  annotations:
    notes: testing
spec:
  serving:
    variants:
      - name: v1
        endpoint: http://app-deploy-prediction-v1.ml.svc.cluster.local:8080
        weight: 100
  timeout: 5s
  rate_limit:
    rps: 100
