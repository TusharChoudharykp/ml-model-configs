metadata:
  name: future-prediction
  labels:
    team: jira
    owner: j@gmail.com
  annotations:
    notes: working fine now
spec:
  serving:
    variants:
      - name: v1
        endpoint: http://future-prediction-v1.ml.svc.cluster.local:8080
        weight: 100
  timeout: 5s
  rate_limit:
    rps: 100
