metadata:
  name: license-number-prediction
  labels:
    team: mstii
    owner: m@gmail.com
  annotations:
    notes: checking
spec:
  serving:
    variants:
      - name: v1
        endpoint: http://license-number-prediction-v1.ml.svc.cluster.local:8080
        weight: 100
  timeout: 5s
  rate_limit:
    rps: 100
