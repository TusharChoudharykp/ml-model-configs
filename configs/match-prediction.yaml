metadata:
  name: match-prediction
  labels:
    team: ml-team
    owner: ml@gmail.com
  annotations:
    notes: testing 3
spec:
  serving:
    variants:
      - name: v1
        endpoint: http://match-prediction-v1.ml.svc.cluster.local:8080
        weight: 100
  timeout: 5s
  rate_limit:
    rps: 100
