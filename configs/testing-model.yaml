metadata:
  name: testing-model
  labels:
    team: testing team
    owner: test@gmail.com
  annotations:
    notes: testing again
spec:
  serving:
    variants:
      - name: v1
        endpoint: http://testing-model-v1.ml.svc.cluster.local:8080
        weight: 100
  timeout: 5s
  rate_limit:
    rps: 100
